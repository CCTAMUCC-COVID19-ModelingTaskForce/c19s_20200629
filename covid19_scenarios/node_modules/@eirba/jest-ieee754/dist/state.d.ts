import { Context } from './types';
export default class State {
    private currentTestName;
    private snapshotState;
    private error?;
    private hasSnapshot;
    private incrementCounters;
    get count(): number;
    get key(): string;
    markSnapshotsAsCheckedForTest(): void;
    get snapshotIsPersisted(): boolean;
    constructor(context: Context);
    getSnapshot(): string;
    setSnapshot(data: string): void;
    couldAddSnapshot(): boolean;
    updateTally(pass: boolean): void;
    addSnapshot(data: string): void;
}
